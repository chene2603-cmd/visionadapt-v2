import 'package:flutter/material.dart';

void main() => runApp(const VisionAdaptApp());

class VisionAdaptApp extends StatelessWidget {
  const VisionAdaptApp({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      debugShowCheckedModeBanner: false,
      title: 'visionadapt-v2',
      theme: ThemeData(
        useMaterial3: true,
        colorScheme: ColorScheme.fromSeed(seedColor: Colors.deepPurple),
      ),
      home: const Scaffold(
        appBar: AppBar(
          title: Text('visionadapt-v2'),
          centerTitle: true,
        ),
        body: Center(
          child: Column(
            mainAxisAlignment: MainAxisAlignment.center,
            children: const [
              Icon(Icons.deployed_code, size: 64, color: Colors.deepPurple),
              SizedBox(height: 16),
              Text(
                '部署成功！',
                style: TextStyle(fontSize: 20, fontWeight: FontWeight.bold),
              ),
              SizedBox(height: 8),
              Text(
                '由 Termux + Gitee + Vercel 自动构建',
                textAlign: TextAlign.center,
                style: TextStyle(color: Colors.grey),
              ),
            ],
          ),
        ),
      ),
    );
  }
}
